#include <stdio.h>

const char* createSuffixArray(char const *input[]);

int main(int argc, char const argv[])
{
    /* code */
    char input[] = "mississippi$";
    const char suffix = createSuffixArray(input);

    return 0;
}

const char* createSuffixArray(char const *input[]) {
    int SA[10];

    return input;
}